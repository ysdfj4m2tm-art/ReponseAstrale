"use client";
import { useState } from "react";
export function RetractionForm(){const[email,setEmail]=useState("");const[reference,setReference]=useState("");const[message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const response=await fetch("/api/retraction/request",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,reference})});setMessage(response.ok?"Si la commande correspond, votre demande a été enregistrée. Nous vous contacterons après vérification.":"La demande n’a pas pu être enregistrée.");}return <form className="question-form" onSubmit={submit}><label className="field-label">E-mail utilisé lors de l’achat<input type="email" required value={email} onChange={(e)=>setEmail(e.target.value)}/></label><label className="field-label">Référence de commande ou de paiement<input required value={reference} minLength={8} maxLength={200} onChange={(e)=>setReference(e.target.value)}/></label><button className="button">Envoyer ma demande</button>{message&&<p role="status">{message}</p>}</form>}
